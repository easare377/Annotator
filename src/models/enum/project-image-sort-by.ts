/** Available sort orders for the Project Images page. */
export enum ProjectImageSortBy {
  DATE_MODIFIED = 'dateModified',
  DATE_ADDED = 'dateAdded',
  FILENAME = 'filename',
  PROGRESS = 'progress'
}
