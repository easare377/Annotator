/** Image statuses available to the Project Images filter. */
export enum ProjectImageStatus {
  ALL = 'all',
  NOT_DETECTED = 'not-detected',
  DETECTED = 'detected',
  INCOMPLETE = 'incomplete',
  ANNOTATED = 'annotated'
}
